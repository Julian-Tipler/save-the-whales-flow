//I don't think I'm using this because I'm setting rules in the UI instead.
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read: if true;
			allow read, write: if request.auth != null;
    }
  }
}